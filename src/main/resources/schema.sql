CREATE TABLE IF NOT EXISTS guitars (
                                       id INT AUTO_INCREMENT PRIMARY KEY,
                                       brand VARCHAR(255),
    string_count INT,
    price DOUBLE
    );